# Secrets & local environments — never commit
.env
venv/
__pycache__/
*.py[cod]
staticfiles/
*.log
*.zip

# Demo data IS committed so the project runs with images/projects on any clone.
# (db.sqlite3 and media/ are intentionally tracked for portable demo setups.)
